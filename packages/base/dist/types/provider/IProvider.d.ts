export type { JRPCRequest, JRPCResponse, Maybe, RequestArguments, SafeEventEmitter, SafeEventEmitterProvider, SendCallBack, } from "@toruslabs/openlogin-jrpc";
export declare const PROVIDER_EVENTS: {
    INITIALIZED: string;
    ERRORED: string;
};
